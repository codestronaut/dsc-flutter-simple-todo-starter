import 'package:flutter/material.dart';

/*
Constants: store all constants needed in whole app
*/

const dataBoxName = "todo_data";
enum TodoFilter {
  ALL,
  COMPLETED,
  PROGRESS,
}

const Color kTealColor = Color(0xFF7FBDCA);
const Color kAmberColor = Color(0xFFE8AF44);
const Color kBackgroundColor = Color(0xFFF5F5F7);
